<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app_ban_hang">
    <uses-permission android:name="android.permission.INTERNET"/>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_ban_hang">
        <activity android:name=".login_SignUp.Login"></activity>
        <activity android:name=".login_SignUp.Register" />
        <activity android:name=".detail.first_Detail_Layout"></activity>
        <activity android:name=".login_SignUp.MainLogin"/>
        <activity android:name=".detail.activity_order_details"></activity>
        <activity android:name=".view.giohang"></activity>
        <activity android:name=".view.thanh_toan"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.App_ban_hang.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>